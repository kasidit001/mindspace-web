/** Deducted from the dashboard's derived "Total points" (see
 * dashboard.vue's POINTS_PER_LESSON) per Code Lab hint revealed — a real,
 * disclosed cost against that same score, not a fabricated currency. */
export const HINT_POINT_COST = 5
